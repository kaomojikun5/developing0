[project]
name = "pocketswarm-engine"
version = "0.1.0"
description = "PocketSwarm worker-side local inference engine"
readme = "README.md"
requires-python = ">=3.12.13"
dependencies = []

[project.scripts]
pocketswarm-engine = "pocketswarm_engine:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
